# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Build output
node_modules
dist
dist-ssr
*.local

# iPad build artifacts
ios/OonoEnt/Web/*
!ios/OonoEnt/Web/.gitkeep
ios/OonoEnt.xcodeproj
ios/build/
ios/DerivedData/

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Rust / Cargo
target/
**/target/

# Tauri sidecar binaries — recompiled from src-tauri/binaries-src/oono-vm-host.swift
# (keep the Swift source in the repo; commit only what isn't a build artifact)
src-tauri/binaries/oono-vm-host-*

# Bundled-VM artifacts. These are big binary blobs (rootfs.img is ~2 GB)
# and are regenerated by scripts/build-acestream-vm.sh. The manifest stays
# committed so reviewers can see expected SHA-256 sums.
src-tauri/resources/vm/rootfs.img
src-tauri/resources/vm/initrd
src-tauri/resources/vm/kernel
vm-image/kbits/
vm-image/modules/
vm-image/vsock-bridge.bin
vm-image/.cache/
vm-image/vsock-bridge/target/

# Local working notes — research / scratch files. README.md is the
# documentation that actually ships with the repo.
radio.md
sol.md
spec.md
movies.md
free-sports.md
virtualization.md
BUNDLED_VM.md
Hybrid_IPTV_Brief.md

# Local development data
*.db
*.db-journal
*.sqlite
*.sqlite-journal

# macOS code-signing / notarisation
*.cer
*.p12
*.provisionprofile
